{
  "name": "buscador-siga",
  "version": "1.0.0",
  "description": "Buscador rápido del Catálogo SIGA MEF",
  "scripts": {
    "lint": "eslint . --ext .js",
    "format": "prettier --write .",
    "test": "vitest run",
    "dev": "python -m http.server 8000"
  },
  "dependencies": {
    "sql.js": "^1.10.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0"
  }
}
